"""Pipeline and failure handlers for the coordinator.
pipeline_handler runs LangGraph flows; failure_handler suggests fixes via LLM.
"""
# Handlers: pipeline execution, failure handling (LLM suggestion + ask user)
